# 14 — 整库备份、诊断与垃圾回收

**What to build:** 创作者可以把整个实例压缩为可恢复冷快照，诊断并重建可重建投影，以及在明确预览后删除真正孤立的来源对象。

**Blocked by:** 06 — 修订、历史化、停用与永久删除知识; 10 — 无损裂分与合并知识胶囊.

**Status:** ready-for-agent

- [ ] 备份取得维护锁、提交事务并干净关闭 SQLite 后原样压缩实例目录。
- [ ] 压缩失败安全恢复写入，不生成增量、自动轮换或隐式加密机制。
- [ ] 恢复解压到新目录并通过只读 Doctor 后才允许切换。
- [ ] 显式 repair 只重建 FTS、证据图、树摘要和其他投影。
- [ ] 规范内容缺失或哈希错误时进入受限只读并报告，不能猜测补造。
- [ ] GC 计划保护所有生命周期状态引用的历史证据，并在显式执行后保留最小删除标记。

## Comments
