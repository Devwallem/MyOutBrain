[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "myoutbrain"
version = "0.1.0"
description = "A local-first personal cognitive library."
requires-python = ">=3.13"
dependencies = []

[project.scripts]
myoutbrain = "myoutbrain.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.mypy]
python_version = "3.13"
strict = true
files = ["src", "tests"]

